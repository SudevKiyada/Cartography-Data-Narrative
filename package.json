{
  "name": "cartography-data-narrative",
  "source": [
    "src/index.html"
  ],
  "targets": {
    "default": {
      "distDir": "./docs",
      "publicUrl": "./",
      "sourceMap": false
    }
  },
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel serve --open",
    "build": "parcel build"
  },
  "devDependencies": {
    "parcel": "^2.6.2"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.5",
    "bootstrap": "^5.1.3",
    "motion": "^10.11.0"
  }
}
